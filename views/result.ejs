<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>PageSpeed Result</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-5">
  <div class="container">
    <h1>PageSpeed Score for <code><%= url %></code></h1>
    <p class="lead">Overall Score: 
      <span class="badge bg-<%= score >= 90 ? 'success' : score >= 50 ? 'warning' : 'danger' %>">
        <%= score %>
      </span>
    </p>
    <h3>Key Metrics</h3>
    <ul class="list-group">
      <% for (let [name, value] of Object.entries(metrics)) { %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <%= name %>
          <span class="badge bg-<%= value < 1000 ? 'success' : value < 3000 ? 'warning' : 'danger' %>">
            <%= value %> ms
          </span>
        </li>
      <% } %>
    </ul>
    <div class="mt-4">
      <a href="/" class="btn btn-secondary">Analyze Another</a>
    </div>
  </div>
</body>
</html>
